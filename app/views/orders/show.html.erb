<h1>Congrats on your order, <%= @order.user.name %>!</h1>
<p>Here's what you ordered:</p>
<table class="cartTable">
	<tr>
		<th>Item</th>
		<th>Quantity</th>
	</tr>
	<% @order.carted_dishes.each do |carted_dish| %>
	<tr>
		<td><a href="/dishes/<%= carted_dish.dish_id %>"><%= carted_dish.dish.name %></a></td>
		<td><%= carted_dish.quantity %></td>
<% end %>
</table>
<p>Order ID: <%= @order.id %></p>
<p>Subtotal: <%= @order.subtotal %></p>
<p>Tax: <%= @order.tax %></p>
<p>Total: <%= @order.total %></p>
<p>An email confirmation will one day be sent to: <%= @order.user.email %></p>
<p>Purchased on: <%= @order.pretty_time %></p>
